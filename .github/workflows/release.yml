name: Build & Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact: logscrub
            asset_name: logscrub-linux
          - os: windows-latest
            artifact: logscrub.exe
            asset_name: logscrub-windows.exe

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install PySide6 platformdirs pyinstaller pyyaml

      - name: Build binary
        run: pyinstaller logscrub.spec --clean

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: dist/${{ matrix.artifact }}

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download Linux binary
        uses: actions/download-artifact@v4
        with:
          name: logscrub-linux
          path: artifacts/

      - name: Download Windows binary
        uses: actions/download-artifact@v4
        with:
          name: logscrub-windows.exe
          path: artifacts/

      - name: Rename artifacts
        run: |
          mv artifacts/logscrub artifacts/logscrub-linux
          mv artifacts/logscrub.exe artifacts/logscrub-windows.exe

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/logscrub-linux
            artifacts/logscrub-windows.exe
          generate_release_notes: true
